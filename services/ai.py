def detect_product_from_image(image_bytes=None):
	# placeholder: in a real system, run a model on image bytes
	# return a product name and confidence
	return {"name": "Product A", "confidence": 0.95}
